type Old_trace
type Latent
type Init_type = real_u /\ $

proc Guide[a: real; b: preal]() -> unit | old : Old_trace | lat : Latent = {
  _ <- observe{old}(NORMAL(a; b));
  weight <- sample{lat}(NORMAL(a; b));
  return ()
}

Initial_type: Init_type
Guide_composition: Guide
